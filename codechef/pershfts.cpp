#include <bits/stdc++.h>
#define N 100005
using namespace std;
int p[N], q[N], r[N];
int main() {
	int t, n, k;
	scanf("%d", &t);
	while(t--){
		scanf("%d%d", &n, &k);
		for(int i = 0 ; i < n ; i++)
			scanf("%d", &p[i]);
		for(int i = 0 ; i < n ; i++)
			scanf("%d", &q[i]);
	}
}
#include <bits/stdc++.h>
using namespace std;
int main(){
	int t, n, **a, ****dp;
	scanf("%d", &t);
	while(t--){
		scanf("%d", &n);
		a = new int*[n];
		dp = new int***[n];
		for(int j = 0 ; j < n ; j++){
			dp[i] = new int**[n]
		}
		for(int i = 0 ; i < n ; i++){
			a[i] = new int[n];
			dp[i] = new int[n];
			for(int j = 0 ; j < n ; j++){
				scanf("%d", &a[i][j]);

			}
		}
	}
}
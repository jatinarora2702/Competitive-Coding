#include <bits/stdc++.h>
#define N 100000
using namespace std;
vector < pair < int , int > > g[N];
priority_queue < int > q;
int n, m;
int main() {
	int x, y, w;
	for(int i = 0 ; i < n ; i++){
		scanf("^d%d", &n, &m);
	}
	for(int  i=  0 ; i < n ; i++){}
		
}